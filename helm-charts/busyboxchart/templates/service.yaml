apiVersion: v1
kind: Service
metadata:
  name: {{ include "busyboxchart.fullname" . }}
  labels:
    {{- include "busyboxchart.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "busyboxchart.selectorLabels" . | nindent 4 }}
